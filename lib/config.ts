/**
 * Central Configuration for the Interactive Quiz App
 * You can toggle features here easily (Simulating an Admin Panel)
 */
export const APP_CONFIG = {
    // If true, Practical (codeoutput) questions are graded automatically and count towards the score.
    // If false, they are treated as theory questions (Self-Assessed / مراجعة).
    GRADE_PRACTICAL: true,
};
